<h1>Listing event_charities</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @event_charities.each do |event_charity| %>
  <tr>
    <td><%= link_to 'Show', event_charity %></td>
    <td><%= link_to 'Edit', edit_event_charity_path(event_charity) %></td>
    <td><%= link_to 'Destroy', event_charity, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Event charity', new_event_charity_path %>
